# yaml-language-server: $schema=https://coderabbit.ai/integrations/schema.v2.json
language: ko-KR
reviews:
  high_level_summary_instructions: |
    ## 종합 리뷰

    ### 검토 항목
    - **개요**: PR 목표와 영향도
    - **설계**: 컴포넌트 분리, 확장성
    - **코드**: TypeScript, 네이밍, Export 규칙, 타입 안전성
    - **스타일**: Tailwind, Import 정렬, Prettier
    - **기능**: 동작성, 엣지 케이스, 에러 처리
    - **UX**: 접근성(WCAG)
    - **성능**: 리렌더링, 번들 크기, 이미지 최적화
    - **보안**: XSS, CSRF, API 보안

    ### 피드백 포맷
    **문제 사항만** 상태 ⚠️ / ❌와 함께 보고
    - 통과한 항목(✅)은 제외
    **내용** + **제안**
    - 내용과 제안은 핵심 내용만 담아서 간결하게 작성
    - 내용과 제안에는 why와 how 포함
  collapse_walkthrough: true
  poem: false
  path_instructions:
    - path: "**/*.config.{ts,js}"
      instructions: |
        **설정 파일 규칙:**
        - Docstring 검사 무시
        - 설정 파일은 코드 리뷰 대상 제외
    - path: src/app/**/page.tsx
      instructions: |
        CLAUDE.md → "🎨 코딩 컨벤션" → "Export 규칙", "React 규칙" 참고
    - path: src/app/**/layout.tsx
      instructions: |
        CLAUDE.md → "🎨 코딩 컨벤션" → "Export 규칙", "React 규칙" 참고
    - path: src/app/**/_components/**/*.tsx
      instructions: |
        CLAUDE.md → "🎨 코딩 컨벤션" → "Export 규칙", "React 규칙" 참고
        (페이지 전용 컴포넌트는 _components/ 디렉토리에만 위치, 재사용 가능하면 src/components/ 이동)
    - path: src/app/**/_hooks/**/*.ts
      instructions: |
        CLAUDE.md → "🎨 코딩 컨벤션" → "함수 선언 방식" 참고
        (_hooks/ 디렉토리에 위치, 재사용 가능하면 src/services/hooks/ 이동)
    - path: src/app/**/_utils/**/*.ts
      instructions: |
        CLAUDE.md → "🎨 코딩 컨벤션" → "함수 선언 방식" 참고
        (_utils/ 디렉토리에 위치, 재사용 가능하면 src/utils/ 이동)
    - path: src/components/**/*.tsx
      instructions: |
        CLAUDE.md → "🎨 코딩 컨벤션" → "Export 규칙", "React 규칙", "Tailwind CSS 규칙" 참고
    - path: src/assets/**/*
      instructions: |
        CLAUDE.md → "📁 폴더 구조" 참고
    - path: src/components/**/*.stories.tsx
      instructions: |
        CLAUDE.md → "### 상수 컨벤션" → "#### 📋 예외: 공식 문서 및 일반적 관례 따르는 경우" 참고
    - path: src/stores/**/*.ts
      instructions: |
        CLAUDE.md → "🎨 코딩 컨벤션" → "Naming Conventions", "TypeScript 규칙" 참고
    - path: src/services/api/**/*.ts
      instructions: |
        CLAUDE.md → "🎨 코딩 컨벤션" → "함수 선언 방식", "TypeScript 규칙" 참고
    - path: src/services/endpoints.ts
      instructions: |
        CLAUDE.md → "### 상수 컨벤션" 참고
    - path: src/services/hooks/**/*.ts
      instructions: |
        CLAUDE.md → "### 상수 컨벤션" → "#### 📋 예외: 공식 문서 및 일반적 관례 따르는 경우" 참고
        (React Query queryKey는 공식 관례에 따라 배열 형식)
    - path: src/types/**/*.ts
      instructions: |
        CLAUDE.md → "🎨 코딩 컨벤션" → "TypeScript 규칙" 참고
    - path: src/utils/**/*.ts
      instructions: |
        CLAUDE.md → "🎨 코딩 컨벤션" → "함수 선언 방식", "Naming Conventions" 참고
    - path: src/constants/**/*.ts
      instructions: |
        CLAUDE.md → "### 상수 컨벤션" 참고
    - path: src/app/api/**/*.ts
      instructions: |
        CLAUDE.md → "🎨 코딩 컨벤션" → "Export 규칙", "TypeScript 규칙" 참고
    - path: src/services/api/notion/**/*.ts
      instructions: |
        CLAUDE.md → "🎨 코딩 컨벤션" → "TypeScript 규칙" 참고
    - path: "**/*.tsx"
      instructions: |
        CLAUDE.md → "🎨 코딩 컨벤션" → "Tailwind CSS 규칙" 참고
    - path: "**/*.ts"
      instructions: |
        CLAUDE.md → "🎨 코딩 컨벤션" → "TypeScript 규칙" 참고
  auto_review:
    base_branches:
      - main
      - develop
issue_enrichment:
  auto_enrich:
    enabled: true
